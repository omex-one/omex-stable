[profile.default]
src = 'src'
test = 'test'
script = 'script'
out = 'out'
libs = ['lib']
cache = true
cache_path = 'cache'
broadcast = 'broadcast'

gas_reports = ["*"]
gas_reports_ignore = [""]

[rpc_endpoints]
blastTest = "${BLAST_SEPOLIA_RPC_URL}"
baseTest = "${BASE_SEPOLIA_RPC_URL}"

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
